# Advanced Usage

This page exercises a nested nav group (`Guides`), which should be rendered as a
subsection under the "Guides" page in the documentation detail view.

## Custom configuration

You can override `docs_dir` in `mkdocs.yml` to point to any folder relative to the
`mkdocs.yml` file itself.
