# Test Product

Welcome to the **Test Product** documentation. This page exists to validate that the
GitHub docs scanner can find `mkdocs.yml`, resolve `docs_dir`, and load content from the
`en` language folder.

## What is this?

A small sample product used purely for testing the documentation pipeline.
