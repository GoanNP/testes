# Betrieb

Routineaufgaben zur Pflege der Testprodukt-Dokumentation:

- Sicherstellen, dass der Ordner `en` immer als Fallback-Sprache existiert.
- `nav:`-Pfade relativ zu `docs_dir` halten, nicht zum Sprachordner.
- Nach dem Push neuer Markdown-Dateien den Scan erneut ausführen (Cache läuft nach
  5 Minuten ab).
